# @package _global_

defaults:
  - override /hydra/sweeper: optuna_sweeper
  - override /dataset: cifar10
  - override /trainer: default
  - override /model: densenet

experiment:
  name: cifar10_optuna
  tags: ["optuna", "cifar10", "sweep"]

trainer:
  max_epochs: 10