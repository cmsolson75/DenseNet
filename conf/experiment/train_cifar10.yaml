# @package _global_
experiment:
  name: cifar10_training
  tags: ["cifar10", "densenet", "paper"]

defaults:
  - override /dataset: cifar100

trainer:
  max_epochs: 300

model:
  drop_prob: 0.2
  block_config: [16, 16, 16]